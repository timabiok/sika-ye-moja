# Layer 1 — Golden image pipeline

Alias of `environments/prod/` — SIG + Azure Image Builder + Ansible layer-1 baseline.

See [../../README.md](../../README.md) for the three-layer model.

Apply here first; consume `sig_image_version_id` in `environments/workload/`.
